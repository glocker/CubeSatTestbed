"""
SocketCAN transport: sends/receives raw frames over vcan0 (local testing) or
a real CAN adapter (hardware-in-the-loop).

Planned later: UDP/TCP transport for purely software-to-software node pairs
that don't need a real bus at all.
"""
