# Planned services (not implemented yet):
#
# framework  - builds from the root Dockerfile, mounts configs/, exposes the
#              API port, attaches to the vcan interface
# vcan-setup - one-shot container/script that creates and brings up vcan0
#              before the framework starts
# openmct    - optional, only added if bridges/openmct is implemented
