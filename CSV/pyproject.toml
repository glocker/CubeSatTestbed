[project]
name = "cubesat-submodules-virtualizer"
version = "0.1.0"
description = "Modular CubeSat subsystem emulation / hardware-in-the-loop test framework"
requires-python = ">=3.11"
# planned dependencies: fastapi, uvicorn, python-can, pydantic, pyyaml
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
