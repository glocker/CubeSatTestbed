# OpenMCT bridge (optional, secondary)

Bridges the backend's telemetry WebSocket to NASA OpenMCT for dashboard
visualization. Not core to the project -- this framework cares more about
scenario running, packet tracing, and test reports than long-term telemetry
dashboards, so this stays optional and is planned after the core engine and
at least one module are working end-to-end.
