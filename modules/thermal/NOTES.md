# Thermal module -- planned, not in v1

Simple RC-style model: temperature responds to heater command state and a
configurable external heat input (stand-in for solar exposure).

Left out of v1 in favor of the Payload module, which exercises cross-module
interaction more directly -- but explicitly kept as a near-term planned
module (not dropped) since it's cheap to add once the module contract is
stable.
