"""DUT/node mode orchestration.

The manager maps each configured node to one of the supported modes:
``simulated``, ``software`` or ``hardware``. Switching the DUT is a config change,
not a code change.
"""
